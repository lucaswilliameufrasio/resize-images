{
  "scripts": {
    "build": "tsc",
    "resize": "node dist/resize.js",
    "build-run": "rimraf dist && npm run build && npm run resize"
  },
  "dependencies": {
    "exifr": "^6.0.0",
    "sharp": "^0.26.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.13",
    "@types/sharp": "^0.26.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
