{
  "scripts": {
    "build": "tsc",
    "resize": "node dist/resize.js",
    "vrum": "rimraf dist && npm run build && npm run resize"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "sharp": "^0.30.7"
  },
  "devDependencies": {
    "@types/node": "^18.0.3",
    "@types/sharp": "^0.30.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}
