{
  "scripts": {
    "build": "tsc",
    "resize": "node dist/resize.js",
    "build-run": "rimraf dist && npm run build && npm run resize"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "sharp": "^0.29.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.10",
    "@types/sharp": "^0.29.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
